// something that ties together the Value (state), the code to be executed, Some Set rules (via tag/label?) this must be able to support TAM2, and Some context (probably the datums that are allowed as read-only during execution))
